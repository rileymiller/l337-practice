package main;

import java.util.ArrayList;

public class Solution {
    public ListNode mergeKLists(ListNode[] lists) {
        ListNode splice = new ListNode(0);
        ArrayList<ListNode> nodeTraversals = new ArrayList<>();

        // Loop through array and create a pointer to each list in the array of lists and push to nodeTraversals

        // Loop while all lists aren't null, loop through nodeTraversals to find the min val
        return splice;
    }
}
